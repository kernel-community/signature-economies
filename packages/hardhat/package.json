{
  "name": "hardhat",
  "version": "1.0.0",
  "description": "Meaningful economies of sign",
  "author": "cryptowanderer",
  "license": "MIT",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.4",
    "@nomiclabs/hardhat-waffle": "^2.0.2",
    "@openzeppelin/contracts": "^4.4.2",
    "chai": "^4.3.6",
    "dotenv": "^14.3.2",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.3",
    "hardhat": "^2.8.3",
    "hardhat-deploy": "^0.11.4",
    "hardhat-deploy-ethers": "^0.3.0-beta.13"
  },
  "scripts": {
    "clean": "yarn hardhat clean",
    "compile": "yarn hardhat compile",
    "localnode": "yarn hardhat node",
    "watch": "yarn hardhat node --watch",
    "test": "yarn hardhat test",
    "coverage": "yarn hardhat coverage",
    "accounts": "yarn hardhat accounts",
    "checksol": "yarn hardhat check",
    "deploylocal": "yarn hardhat --network localhost deploy --export-all ../dapp/src/deployments.json,../subgraph/config/deployments.json",
    "mintlocal": "yarn hardhat --network localhost mint --tokenuri https://arweave.net/R5VjN9UOc1llzmvOYvymFmDexZmIxIkzz9n5CvyVAd8",
    "receivelocal": "yarn hardhat --network localhost receiveDonation --selectednft three",
    "deployrinkeby": "yarn hardhat --network rinkeby deploy --export-all ../dapp/src/deployments.json,../subgraph/config/deployments.json"
  }
}
